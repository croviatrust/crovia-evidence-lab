# CRC-1 — Crovia Artifact Contract

CRC-1 defines a **deterministic artifact contract** for Crovia evidence outputs.

Each CRC-1 pack is:
- reproducible
- verifiable offline
- self-contained
- indexed by a MANIFEST.json

CRC-1 is an **output contract**, not a workflow or pipeline.
